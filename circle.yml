machine:
  ruby:
    version: 2.2.2
#  java:
#    version: oraclejdk8
dependencies:
  cache_directories:
    - "tmp"
  post:
    - mv config/config.example.yml config/config.yml
#database:
#  post:
#    - bundle exec rake jetty:clean
#test:
#  override:
#    - bundle exec rake ci
