machine:
  ruby:
    version: 2.2.2
#  java:
#    version: oraclejdk8
dependencies:
  cache_directories:
    - "tmp"
  pre:
    - gem install fcrepo_wrapper
    - gem install solr_wrapper
  post:
    - fcrepo_wrapper --no-jms -p 9984:
      background: true
    - solr_wrapper -n hydra-test -p 9983:
      background: true
    - sleep 10
