machine:
  ruby:
    version: 2.2.2
#  java:
#    version: oraclejdk8
dependencies:
  cache_directories:
    - "tmp"
  post:
    - fcrepo_wrapper --no-jms:
        background: true
    - solr_wrapper -n hydra-test:
        background: true
    - sleep 5
