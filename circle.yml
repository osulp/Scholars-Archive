machine:
  ruby:
    version: 2.2.2
#  java:
#    version: oraclejdk8
dependencies:
  cache_directories:
    - "tmp"
  post:
    - bundle exec fcrepo_wrapper --no-jms -p 9984 > ./log/fcrepo_wrapper.log 2>&1:
      background: true
    - bundle exec solr_wrapper -n hydra-test -p 9983 > ./log/solr_wrapper.log 2>&1:
      background: true
    - sleep 10
