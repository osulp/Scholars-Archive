<% if f.object.multiple? key %>
    <%= f.input key, as: :multi_value, input_html: { class: "form-control publisher-default #{f.object.new_record? ? 'new-record' : ''}" }, required: f.object.required?(key) %>
<% else %>
    <%= f.input key, required: f.object.required?(key) %>
<% end %>