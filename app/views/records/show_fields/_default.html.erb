<% Array(record[key]).each do |v| %>
  <span itemprop="<%= key %>" itemscope itemtype="http://schema.org/Thing">
    <%= link_to_facet(v, Solrizer.solr_name(key, :facetable)) %>
  </span>
  <br />
<% end %>
